{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "start": "lerna run start --stream",
    "prettier": "lerna run format --stream",
    "format": "npm run prettier",
    "kill": "npx kill-port 8888 8886 8887 9000 35886 35887 35888 35900"
  }
}
